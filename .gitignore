node_modules/
dist/
dist-calc/
*.tsbuildinfo
.DS_Store
coverage/

# Local env files (Supabase keys etc.) — never commit
.env
.env.local
.env.*.local

# Local KiCad source/symbol clones used only as porting references
kicad-src/
kicad-symbols-src/

# R2 upload staging built from kicad-src (never committed)
tools/demos/out/

# Production defaults (public values only — secrets go in Vercel env / .env.local).
# The hosted 3D-model library: the full KiCad 10 packages3D set pre-converted to
# .glb (see tools/models3d), served from Cloudflare R2. The r2.dev URL is fine
# while traffic is small; swap for a bucket custom domain when it grows.
VITE_MODELS3D_URL=https://pub-ac941e05e1284f409be2ed74ddb151b3.r2.dev
